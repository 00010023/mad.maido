<?xml version="1.0" encoding="utf-8"?>

<!-- Copyrighted to 00010023 UwUssimo in order to avoid plagiarism issues -->

<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.uwussimo.maido">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Maido"
        android:fullBackupContent="true">
        <activity
            android:name=".Middlewares.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Middlewares.MainActivity"
            android:exported="true" />
    </application>

</manifest>